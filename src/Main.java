import java.net.*;
import java.io.*;

import tcp.Tcp;
import java.util.LinkedList;
import java.util.List;

public class Main {
    public static void main(String[] args) throws IOException {
        List<Tcp> connections = new LinkedList<>();
        while (true) {
            Tcp currentConnection = new Tcp()
            server.waitForConnection();
        }
    }
}